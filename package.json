{
  "name": "@vaep/root",
  "version": "0.0.0",
  "private": true,
  "description": "Vertical AI Employee Platform (V-AEP) monorepo",
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.5.4"
  },
  "pnpm": {
    "overrides": {
      "vite": "^5.4.8"
    }
  }
}
